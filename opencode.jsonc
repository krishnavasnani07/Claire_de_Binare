{
  "$schema": "https://opencode.ai/config.json",
  // cdb_context is intentionally left to user-local OpenCode MCP config.
  // OpenCode 1.1.20 does not cleanly support `cwd` in this config path,
  // and the repo-local Python entrypoint needs the working directory set.
  "mcp": {}
}
