# Root dependency layer for repo-level CI/test and convenience flows.
# Active runtime canon lives in service-local requirements plus real Dockerfile
# install paths; root-only bumps are not equivalent to runtime reconciliation.
# Async Support
# Claire de Binare Trading Bot
# Core
# Crypto & Signing (für MEXC API)
# Database
# Environment Variables
# Execution Service - Dependencies
# HTTP Client (für MEXC API)
# Logging
# Paper Trading Runner Dependencies
# Redis Client
# Signal Engine - Requirements
# Utilities
# Web Framework
aiohttp>=3.13.3
cryptography==46.0.5
flask==3.1.3
werkzeug>=3.1.4  # Security: Fix CVEs in Dependabot alerts #339
psycopg2-binary==2.9.11
python-dateutil==2.8.2
python-dotenv==1.2.2
python-json-logger==4.0.0
prometheus_client==0.21.1
redis==7.2.0
requests==2.32.5
