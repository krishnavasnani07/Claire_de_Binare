# Issue #342 Minimal Patch - Redis Publisher Integration

## Problem
cdb_ws mexc_v3_client empfängt Trades, publiziert NICHTS nach Redis (Zeile 101-102 TODO).

## Solution (3 Files)

### 1. services/ws/service.py
**Änderungen:**
- Import redis (Zeile 18+)
- Global redis_client (Zeile 35+)
- In run_mexc_client(): Redis Connection + Publisher Callback
- In on_trade(): redis.publish("market_data", json.dumps(event))
- /health: Redis Status ergänzen
- Metrics: redis_publish_total, redis_publish_errors_total

### 2. services/ws/mexc_v3_client.py
**KEINE Änderung** – Client bereits production-ready.

### 3. infrastructure/compose/base.yml (oder dev.yml)
**Änderungen:**
- cdb_ws: environment: REDIS_HOST, REDIS_PORT (falls noch nicht vorhanden)
- cdb_ws: depends_on: cdb_redis

## Evidence Required (Post-Patch)
- Redis MONITOR zeigt PUBLISH market_data {"source":"mexc",...}
- cdb_signal Logs zeigen eingehende market_data Messages
- Prometheus: decoded_messages_total > 0, redis_publish_total > 0
- Grafana: 4 Panels aus Issue #342 (ws_connected=1, redis_publish_total rate > 0)

## Risk
MINIMAL – Redis Connection bereits in allen anderen Services verwendet (signal, risk, execution).
