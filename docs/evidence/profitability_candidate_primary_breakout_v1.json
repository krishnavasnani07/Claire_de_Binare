{
  "schema_version": "profitability_candidate_contract.v1",
  "candidate_id": "cand-primary-breakout-v1-btcusdt-mexc-3091",
  "strategy_family": "primary_breakout",
  "symbol_universe": [
    "BTCUSDT"
  ],
  "timeframe": "1m",
  "direction": "long_only",
  "regime_scope": {
    "allowed_regimes": [
      "TREND"
    ],
    "blocked_regimes": [
      "RANGE",
      "PANIC",
      "UNKNOWN"
    ],
    "freshness_required": true
  },
  "parameter_set": {
    "entry_lookback_minutes": 240,
    "exit_lookback_minutes": 120,
    "breakout_buffer": 0.0005,
    "min_minutes_between_entries": 60,
    "trade_side_mode": "long_only"
  },
  "hypothesis": "BTCUSDT trend breakouts on MEXC 1m candles can produce net-positive expectancy when regime filtering is active and execution friction is accounted for. This candidate uses repo-live primary_breakout_v1 config values as documented in core/contracts/primary_breakout_v1_config.py.",
  "risk_assumptions": [
    "Core Risk Service, kill-switch, and LR gates remain authoritative.",
    "Candidate evidence cannot override runtime risk blocks."
  ],
  "execution_assumptions": [
    "Fees, spread, and slippage must be accounted for before ranking.",
    "Backtest, ARVP, and paper evidence do not imply live execution readiness.",
    "File-backed replay on this candidate requires regime_id assignment; currently blocked by that gap."
  ],
  "status": "SPECIFIED",
  "allowed_next_gate": "BACKTESTED",
  "reject_reason": null,
  "unsafe_zones": [
    {
      "zone": "live-capital",
      "reason": "LR remains NO-GO and this contract is research-only."
    }
  ],
  "limitations": [
    "regime_id is null in the target MEXC dataset. Candidate cannot currently be BACKTESTED because the historical bridge requires integer regime_id values per row.",
    "Evidence packet is partial: replay dry-run passes but full backtest is blocked.",
    "Single-symbol (BTCUSDT) scope only."
  ]
}
