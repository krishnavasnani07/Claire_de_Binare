# Sample Output: Rotate-Secrets.ps1 export

**Command:**
```powershell
.\tools\secrets\Rotate-Secrets.ps1 export
```

**Output:**

```
======================================================================
  CDB Secret Rotator v1.0
======================================================================
[INFO] Command: export
[INFO] Manifest: D:\Dev\Workspaces\Repos\Claire_de_Binare\tools\secrets\secrets.manifest.json

======================================================================
  Validating Manifest (Fail-Closed)
======================================================================
[OK]   Manifest version: 1
[OK]   Secrets path exists: C:\Users\janne\Documents\.secrets\.cdb
[OK]   Secrets defined: 4
[OK]   Manifest validation passed (fail-closed checks OK)

======================================================================
  Export: Generating .env.runtime
======================================================================
[INFO] Export REDIS_PASSWORD (length: 32)
[INFO] Export POSTGRES_PASSWORD (length: 32)
[INFO] Export POSTGRES_PASSWORD_DSN (length: 32)
[OK]   Created: D:\Dev\Workspaces\Repos\Claire_de_Binare\tools\secrets\.env.runtime

======================================================================
  Export Complete
======================================================================
[INFO] Next step: Restart stack: docker compose -f infrastructure/compose/compose.blue.yml up -d && docker compose -f infrastructure/compose/compose.red.yml up -d

```

---

## Generated File: .env.runtime

**Location:** `tools/secrets/.env.runtime`

**Status:** ✅ Created, gitignored

**Content Structure (Values Redacted):**
```bash
# CDB Runtime Secrets (auto-generated by Rotate-Secrets.ps1)
# DO NOT COMMIT - gitignored

REDIS_PASSWORD=<base64-encoded-32-bytes>
POSTGRES_PASSWORD=<base64-encoded-32-bytes>
POSTGRES_PASSWORD_DSN=<base64-encoded-32-bytes>
```

**Format:**
- Plain text ENV format (`KEY=VALUE`)
- No quotes around values
- No trailing newline per line
- Comments at top (ignored by parser)

---

## Key Observations (Safe)

### ✅ Export Success
- **3 secrets exported** (auto secrets only)
- **GRAFANA_ADMIN_PASSWORD excluded** (manual)
- **.env.runtime created** in tools/secrets/

### 🔒 File Safety
- **Gitignored:** `.gitignore` pattern: `*.env.runtime`
- **Location:** Not in repo root (in tools/secrets/)
- **Permissions:** Only readable by current user (Windows ACL)

### 📊 Kanonischer Stack-Start nach Export

```powershell
docker network create cdb_network 2>$null
docker compose -f infrastructure/compose/compose.blue.yml up -d
docker compose -f infrastructure/compose/compose.red.yml up -d
```

Compose liest Secrets direkt aus `$SECRETS_PATH` (`~/Documents/.secrets/.cdb`).
Die `.env.runtime`-Datei ist ein optionaler Export-Zwischenschritt; nicht zwingend für den Start.

> **[LEGACY COMPAT]** `infrastructure/scripts/stack_up.ps1` lädt `.env.runtime` automatisch,
> wenn sie vorhanden ist (`CDB_IGNORE_RUNTIME_ENV=1` deaktiviert das).
> Dieser Pfad ist nicht mehr der kanonische Operator-Startflow.

---

## Cleanup (Optional)

To remove runtime exports after successful startup:
```powershell
Remove-Item tools\secrets\.env.runtime
```

**Note:** Next rotation will regenerate the file automatically.

---

**Date:** 2026-01-28
**User:** janne
**Tool Version:** v1.0
