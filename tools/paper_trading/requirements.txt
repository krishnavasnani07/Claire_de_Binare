# Paper Trading Runner Dependencies
# Pin versions for deterministic builds

# Web framework (health endpoint)
Flask==3.1.3
Werkzeug>=3.1.4  # Security: Fix CVEs in Dependabot alerts #339

# Database
psycopg2-binary==2.9.9

# Message queue
redis==5.0.1

# Data processing
pandas==2.1.4
numpy==1.26.2

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1

# Email (for alerter)
sendgrid==6.11.0
