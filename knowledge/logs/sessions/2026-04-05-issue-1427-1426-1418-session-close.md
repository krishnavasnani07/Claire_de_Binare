# Session 2026-04-05 — #1427 Tests · #1426 Fix · #1418 Parent-Close

## Scope

Drei Issues in einer Session bearbeitet: Regressionstests für #1427 (Disk-Fallback), Healthcheck-Fix für #1426 (cdb_market), Parent-Closure #1418 (P5-Proof-Anchor).

## Ergebnisse

### #1427 — Disk-Evidence-Fallback Regressionstests

- Auf Branch `fix/1427-soak-monitor-disk-evidence-fallback` gewechselt
- `tests/unit/scripts/test_soak_monitor_timeline.py` ergänzt: neue Klasse `TestDiskCheckFallback` + Helpers `is_docker_df_valid()` und `disk_check_mode()`
- Abgedeckt: alle 3 Verdict-Pfade (host_fs_available / fallback_active / disk_unavailable), Regressionsnagel gegen DISK_UNAVAILABLE bei valider Docker-Evidence, bash-Content-Guard
- 124/124 Tests grün
- Commit `6c6c8b64`, PR #1429 — ci PASS, policy-gate FAIL (kein Label gesetzt, da außerhalb Session-Scope)
- **Offener Punkt:** PR #1429 braucht Label `allow-core-change` → policy-gate FAIL

### #1426 — cdb_market unhealthy (503) im LR-040-Preflight

- Root Cause: fehlende `start_period` im Docker-Healthcheck; Flask serviert `/health` vor `_subscription_active = True`
- Ursache-Klasse: Healthcheck-/Readiness-Drift, kein Service-Defekt
- Fix: `infrastructure/compose/compose.blue.yml:91` — `start_period: 30s` hinzugefügt
- Label `infra-only` auf PR #1430 gesetzt → policy-gate grün
- Branch up-to-date gebracht (`gh pr update-branch`), alle Checks PASS
- PR #1430 gemergt (Commit `7793c028`, 2026-04-05T18:46:24Z)
- Issue #1426 geschlossen

**Side-Issue-Kandidaten (nicht adressiert):**
- Kein Reconnect-Versuch bei Redis-Startup-Failure (`services/market/service.py:352–355`)
- `cdb_candles`, `cdb_regime`, `cdb_allocation`: gleiche `start_period`-Klasse

### #1418 — P5-Proof-Pfad Parent-Anchor

- Alle 5 Child-Issues (#1419–#1423) bereits CLOSED
- `manifest.json` dreiweg-verifiziert: Working-tree SHA-256 = Manifest SHA-256 ✓
- Vorheriger Kommentar hatte 3 Checksums als „falsch" markiert — ist Windows-CRLF-vs-LF-Artefakt, kein realer Content-Drift
- Checksum-only-Edit korrekt abgelehnt; kein Patch nötig
- Manifest: `package_status: GO`, `lr040_verdict: PASS`, `all_required_paths_present: true`
- Issue #1418 geschlossen

## Offene Punkte

- PR #1429 braucht Label `allow-core-change` → dann policy-gate grün, Merge möglich

## Commits / PRs

- `6c6c8b64` — test(soak): regression tests for #1427
- `7793c028` — fix(market): add start_period to cdb_market healthcheck (#1426) — auf main
