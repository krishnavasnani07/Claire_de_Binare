# Shadow Run Manifest
**Run ID**: `shadow-20260123-074916`
**Created**: 2026-01-23 07:49:16 UTC
**Status**: READY TO START
**Phase**: Shadow Mode (4h minimum)
**Sprint Context**: Post Sprint 2 Part 2 (#620)

---

## Run Configuration

### Bot ID (Pipeline Isolation)
```bash
BOT_ID=shadow-20260123-074916
```

### Duration Target
- **Minimum**: 4 hours
- **Start Time**: TBD (manual start)
- **Expected End**: TBD (start + 4h)

### Mismatch Budget
- **Budget**: 5 (aligned with E2E default)
- **Source**: Default (no env override)
- **Gate**: FAIL if `abs(order_results - trades) > 5`

---

## Service Configuration Changes

### Method 1: Environment Variable Override (Recommended)

Add to Docker Compose override or shell environment:

```bash
# Set shadow bot_id for all services
export CDB_BOT_ID=shadow-20260123-074916

# Disable real exchange API calls (mock mode)
export CDB_EXCHANGE_MODE=shadow

# Optional: Increase logging
export LOG_LEVEL=INFO
```

Then restart services:
```bash
docker compose -f infrastructure/compose/base.yml \
  -f infrastructure/compose/dev.yml \
  restart cdb_signal cdb_risk cdb_execution cdb_db_writer
```

### Method 2: Config File Override (Alternative)

Edit service configs to set `bot_id` temporarily:

```yaml
# config/signal.yml (example)
bot_id: shadow-20260123-074916
exchange:
  mode: shadow  # Prevents real API calls
```

**IMPORTANT**: Revert these changes after shadow run completes.

---

## Pre-Flight Checklist

Before starting shadow run:

- [ ] Docker stack running (`docker compose ps`)
- [ ] Redis accessible (`docker exec cdb_redis redis-cli ping`)
- [ ] Postgres accessible (`docker exec cdb_postgres pg_isready`)
- [ ] Grafana dashboards available (http://localhost:3000)
- [ ] No production trades in progress (check `stream.order_results`)
- [ ] Services healthy (no recent crash loops)

---

## Monitoring Setup

### Grafana Dashboard Filters

Add these filters to existing dashboards:

```promql
# Order results for this shadow run
{bot_id="shadow-20260123-074916"}

# Trades for this shadow run (if exposed as metric)
{bot_id="shadow-20260123-074916"}
```

### Monitoring Intervals

| Time | Action |
|------|--------|
| **T+0** (start) | Baseline snapshot (Redis/DB counts) |
| **T+30min** | First checkpoint (>= 5 events expected) |
| **T+1h** | Early validation (>= 10 events target) |
| **T+2h** | Mid-run check (mismatch trend) |
| **T+3h** | Late validation (approaching minimum) |
| **T+4h** | Final audit (hard assertions) |

---

## Real-Time Monitoring Commands

### 1. Check Order Results (Redis Stream)

```bash
# Count order_results for shadow run
docker exec cdb_redis sh -c '
  redis-cli XREVRANGE stream.order_results + - COUNT 1000 | \
  grep -c "shadow-20260123-074916"
'
```

### 2. Check Trades (Postgres DB)

```sql
-- Connect to DB
docker exec -it cdb_postgres psql -U cdb_user -d cdb

-- Count trades for shadow run
SELECT COUNT(*) AS trade_count
FROM trades
WHERE metadata->>'bot_id' = 'shadow-20260123-074916';

-- View sample trades
SELECT id, order_id, symbol, side, status, timestamp
FROM trades
WHERE metadata->>'bot_id' = 'shadow-20260123-074916'
ORDER BY timestamp DESC
LIMIT 5;
```

### 3. Calculate Mismatch (Live)

```bash
# Bash one-liner (run from host)
ORDER_COUNT=$(docker exec cdb_redis sh -c "redis-cli XREVRANGE stream.order_results + - COUNT 1000 | grep -c 'shadow-20260123-074916'")
TRADE_COUNT=$(docker exec cdb_postgres psql -U cdb_user -d cdb -tAc "SELECT COUNT(*) FROM trades WHERE metadata->>'bot_id' = 'shadow-20260123-074916'")
MISMATCH=$((ORDER_COUNT - TRADE_COUNT))
echo "Order Results: $ORDER_COUNT"
echo "Trades: $TRADE_COUNT"
echo "Mismatch: $MISMATCH (Budget: 5)"
```

### 4. Check for DB Duplicates (Integrity)

```sql
-- Detect duplicate order_ids (FAIL condition)
SELECT order_id, COUNT(*) AS duplicate_count
FROM trades
WHERE metadata->>'bot_id' = 'shadow-20260123-074916'
GROUP BY order_id
HAVING COUNT(*) > 1;

-- Expected: 0 rows (empty result = PASS)
```

### 5. Monitor Service Logs (Bypass Detection)

```bash
# Check for E2E bypass activation (FAIL condition)
docker compose logs cdb_risk --since 4h | grep -i "E2E BYPASS"

# Expected: No output (bypass should NOT activate in shadow)
```

---

## Expected Behavior

### Normal Shadow Mode Operation

1. **Market Data Flow**: Live ticks processed as usual
2. **Signal Generation**: Signals created based on regime/risk state
3. **Risk Checks**: HIGH_VOL_CHAOTIC still blocks (no bypass)
4. **Execution**: Orders created but NOT sent to exchange API
5. **Order Results**: Generated with `bot_id=shadow-20260123-074916`
6. **DB Persistence**: Trades written to DB with shadow bot_id

### What You Should See

- Order results appearing in `stream.order_results` with shadow bot_id
- Trades appearing in `trades` table with matching bot_id
- Mismatch staying within budget (0-5 range)
- No E2E bypass log entries
- No actual exchange API calls

### What You Should NOT See

- Real trades on exchange (shadow mode = mock execution)
- E2E bypass activation (production risk behavior)
- Mismatch > 5 (governance gate violation)
- Duplicate order_ids in DB (integrity violation)

---

## Stopping the Shadow Run

### After 4+ Hours

1. **Stop services** (or revert config):
   ```bash
   # If using env var override
   unset CDB_BOT_ID
   unset CDB_EXCHANGE_MODE
   docker compose restart cdb_signal cdb_risk cdb_execution cdb_db_writer
   ```

2. **Run final audit** (see `SHADOW_AUDIT_20260123_074916.md`)

3. **Capture evidence**:
   - Export metrics from Grafana (JSON)
   - Export final mismatch calculation
   - Export DB sample (5 trades)

---

## Rollback Plan

If shadow run fails or needs early termination:

1. **Immediate**: Revert config changes (remove shadow bot_id)
2. **Restart services**: Back to production config
3. **Document**: Record failure reason in audit file
4. **Fix**: Address root cause before retry
5. **Retry**: New shadow run with new timestamp

---

## Next Steps (After Shadow Run)

### If PASS
- Create `SHADOW_AUDIT_20260123_074916.md` with PASS verdict
- Proceed to Soak Phase (24h run)
- New run ID: `soak-{timestamp}`

### If FAIL
- Create `SHADOW_AUDIT_20260123_074916.md` with FAIL verdict
- Analyze failure (mismatch exceeded, duplicates, bypass leak)
- Fix root cause
- Retry shadow with new run ID

---

## Audit Trail

This manifest will be updated with:
- Actual start time
- Actual end time
- Final mismatch value
- PASS/FAIL verdict
- Link to audit file

**Status**: Ready to execute. Waiting for manual start trigger.

---

## Contact / Escalation

If issues arise during shadow run:
- Check `reports/shadow_mode/SHADOW_SOAK_PHASE_DEFINITION.md` for exit criteria
- Review `reports/shadow_mode/SPRINT_2_PART_2_EVIDENCE.md` for E2E baseline
- Escalate if mismatch budget consistently near limit (4-5/5)

---

**Generated**: 2026-01-23 07:49:16 UTC
**Template Version**: 1.0 (aligned with SHADOW_SOAK_PHASE_DEFINITION.md)
