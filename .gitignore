# Claire de Binare local artifacts and generated output

# Historical broad test-file guard. Keep unchanged until scripts/test
# policy drift is cleaned up separately.
# Block ad-hoc test files in root/services
*test*.py
*test*.js
# Allow official test suite in tests/ directory (must come AFTER wildcard)
!tests/**/*.py
!tests/**/*.js
debug_*.py
simple_test.*
quick_test.*
real_test*
*_backup.py

# Generated tool output files (local, do not version)
emoji-report.*
*.emoji-backup

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
!knowledge/logs/
!knowledge/logs/**
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
*.temp

# Local development
.env
.env.local
.env.*.local

# =============================================================================
# SECRETS - CRITICAL: Never commit secrets to repo (Blueprint-compliant)
# =============================================================================
.secrets/
!.secrets.example/
*.secret
*.key
*.pem
*.p12
*.pfx
id_rsa*
*.ppk
credentials
*service-account*.json

# Cloud credentials
.aws/
.azure/
.gcloud/

# Decrypted SOPS files
*.decrypted.*

# Compose directory secrets symlink
infrastructure/compose/.secrets

# =============================================================================

# MCP configuration (contains API keys)
.mcp.json
mcp-config.toml

# Legacy docker-compose files (migrated to infrastructure/compose/)
docker-compose.yml
docker-compose.base.yml
docker-compose.dev.yml

# Cache and local virtualenvs
.cache/
.npm
.eslintcache
.venv/
.pytest_cache/
.ruff_cache/

# Coverage
coverage/
*.lcov

# Dependencies  
node_modules/

# Other projects in workspace (separate repos)
Claire_de_Binare/
Claire_de_Binare_Docs/
.cdb_local/

# AI tooling local state
.claude/
.gemini/
gha-creds-*.json

# Historical root scripts guard. Keep semantics stable until a dedicated
# scripts-policy cleanup removes the need for broad ignore + negation logic.
/scripts/
# LR-003 Contract Drift Guard (P0) - must be versioned
!scripts/
!scripts/lr003_contract_drift_guard.py

# Auto Claude data directory
.auto-claude/

# Git worktrees metadata
.worktrees/
.worktrees_backup/

# Build artifacts
..cdb_local.compose_rendered.yml
*_rendered.yml

# Orchestrator outputs and analysis files
.orchestrator_outputs/
.orchestrator_*.md
*_analysis.json
*_categorization.txt
/.local/

# =============================================================================
# SECRET ROTATION RUNTIME EXPORTS (cdb-secrets-rotator)
# =============================================================================
*.env.runtime
*.runtime.env
.secrets/*.export
tools/secrets/.env.*
!tools/secrets/README.md
!tools/secrets/evidence/**
# Note: .rotation_state.json now lives in $SECRETS_PATH (outside repo) since v1.2

# Phase 1 runtime artifacts (local only)
P1_RUNTIME_DOD_REPORT.md
runtime_evidence_bundle_P1.json

# Local-only ops helper artifact (do not version)
scripts/cdb_ops.ps1

# Decision Contract audit records (generated at runtime + by tests)
reports/decision_contract/
